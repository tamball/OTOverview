name: bible_overview
description: 聖經每卷書概覽（初信友善）— Material 3
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^6.1.0
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.3
  url_launcher: ^6.3.1
  youtube_player_iframe: ^5.2.2
  share_plus: ^12.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/bible/
